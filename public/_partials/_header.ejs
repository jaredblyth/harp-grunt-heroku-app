<% function indexOf(path, slug) { %>
  <% return path.indexOf(slug); %>
<% } %>

<nav id="mainnav">
<ul>
<% for(var slug in public._data){ %>
    
    <li>
        <a href="/<%- slug %>"><%= public._data[slug].menuLabel %></a>
    </li>
    
<% }; %>
</ul>
</nav>